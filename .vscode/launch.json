{
  "version": "0.2.0",
  "configurations": [{
    "type": "node",
    "request": "launch",
    "name": "Launch Program",
    "program": "${workspaceRoot}\\dist\\app.js",
    "outFiles": ["${workspaceRoot}/dist/app.js"], //源映射，
    "preLaunchTask": "build" //首先执行build task
  }]
}
